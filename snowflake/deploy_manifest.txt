# Deploy manifest -- schemas and tables, run in this exact order.
# One file per line. Blank lines and lines starting with # are ignored.
# To skip a file for one deploy, comment it out with # rather than
# deleting the line, so it's easy to re-enable later.

snowflake/01_schemas_and_grants.sql
snowflake/raw_sm/tables/customers.sql
snowflake/raw_sm/tables/products.sql
snowflake/raw_sm/tables/orders.sql
snowflake/raw_sm/tables/order_lines.sql
